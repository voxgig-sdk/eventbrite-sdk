# Eventbrite SDK

Eventbrite Events API client, generated from the OpenAPI spec.

Create, read, and update Eventbrite events.

Learn more about Eventbrite Events API at [eventbriteapi.com](https://eventbriteapi.com).

This is an unofficial SDK for the Eventbrite Events public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Event — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`):

```ts
const client = new EventbriteSDK()
const items = await client.Event().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = EventbriteSDK.test({
  entity: {
    event: {
      test01: { id: 'test01' },
    },
  },
})
const events = await client.Event().list()
// events is an array of Event entities, populated with mock data
// — call events[0].data() for the record itself
console.log(events)
```

### Python

```python
client = EventbriteSDK.test()
events = client.Event().list()
print(events)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = EventbriteSDK::test([
    "entity" => ["event" => ["test01" => ["id" => "test01"]]],
]);
$events = $client->Event()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Event(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Event():list()
```

### JavaScript

```js
const client = EventbriteSDK.test()
const events = await client.Event().list()
// events is an array of entities, populated with mock data
// — call events[0].data() for the record itself
console.log(events)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/eventbrite` | publish pending — [install from git tag](https://github.com/voxgig-sdk/eventbrite-sdk/releases) |
| Python | `voxgig-sdk-eventbrite` | publish pending — [install from git tag](https://github.com/voxgig-sdk/eventbrite-sdk/releases) |
| PHP | `voxgig-sdk/eventbrite` | publish pending — [install from git tag](https://github.com/voxgig-sdk/eventbrite-sdk/releases) |
| Golang | `github.com/voxgig-sdk/eventbrite-sdk/go` | `go get github.com/voxgig-sdk/eventbrite-sdk/go@latest` |
| Lua | `voxgig-sdk-eventbrite` | publish pending — [install from git tag](https://github.com/voxgig-sdk/eventbrite-sdk/releases) |
| JavaScript | `@voxgig-sdk/eventbrite-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/eventbrite-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/eventbrite-sdk/go-cli` | `go install github.com/voxgig-sdk/eventbrite-sdk/go-cli/cmd/eventbrite@latest` |
| Go MCP server | `github.com/voxgig-sdk/eventbrite-sdk/go-mcp` | `go get github.com/voxgig-sdk/eventbrite-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { EventbriteSDK } from '@voxgig-sdk/eventbrite'

const client = new EventbriteSDK({
  apikey: process.env.EVENTBRITE_APIKEY,
})

// List all events (returns EventEntity[] — .data() for the record)
const events = await client.Event().list()
for (const event of events) {
  console.log(event)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o eventbrite-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "eventbrite": {
      "command": "/abs/path/to/eventbrite-mcp"
    }
  }
}
```

## Entities

The API exposes one entity:

| Entity | Description | API path |
| --- | --- | --- |
| **Event** | The Event entity (create, list, load). | `/events` |

The operations available across these entities are **load**, **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from eventbrite_sdk import EventbriteSDK

client = EventbriteSDK({
    "apikey": os.environ.get("EVENTBRITE_APIKEY"),
})

# List all events (returns a list, raises on error)
events = client.Event().list()
for event in events:
    print(event)

# Load a specific event (returns the record, raises on error)
event = client.Event().load({"id": "example_id"})
print(event)
```

### PHP

```php
<?php
require_once 'eventbrite_sdk.php';

$client = new EventbriteSDK([
    "apikey" => getenv("EVENTBRITE_APIKEY"),
]);

// List all events (returns an array; throws on error)
$events = $client->Event()->list();
print_r($events);

// Load a specific event (returns the ENTITY; call data_get() for the record; throws on error)
$event = $client->Event()->load(["id" => "example_id"]);
print_r($event);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/eventbrite-sdk/go"

client := sdk.NewEventbriteSDK(map[string]any{
    "apikey": os.Getenv("EVENTBRITE_APIKEY"),
})

// List all events
events, err := client.Event(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(events)
```

### Lua

```lua
local sdk = require("eventbrite_sdk")

local client = sdk.new({
  apikey = os.getenv("EVENTBRITE_APIKEY"),
})

-- List all events
local events, err = client:Event():list()
print(events)

-- Load a specific event
local event, err = client:Event():load({ id = "example_id" })
print(event)
```

### JavaScript

```js
const { EventbriteSDK } = require('@voxgig-sdk/eventbrite-js')

const client = new EventbriteSDK({
  apikey: process.env.EVENTBRITE_APIKEY,
})

// List all events (returns an array)
const events = await client.Event().list()
for (const event of events) {
  console.log(event)
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://www.eventbriteapi.com/v3](https://www.eventbriteapi.com/v3)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Eventbrite Events API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
